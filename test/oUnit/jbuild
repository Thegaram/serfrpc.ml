(jbuild_version 1)

(executable
  (
    (name runtest)
    (libraries (serfrpc msgpck oUnit))
    (preprocess (pps (lwt_ppx)))
  )
)


(alias
  (
    (name runtest)
    (deps (runtest.exe))
    (action (run ${<}))
  )
)
